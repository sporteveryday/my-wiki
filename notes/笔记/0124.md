字节跳动（ByteDance，包括抖音/今日头条/飞书等）的前端面试中，**算法题**仍然是绕不过去的一环，尤其是一二面 coding 环节。

虽然前端岗位不像后端/算法岗那么“卷”，但字节前端的算法考察**偏中等偏难**，而且**喜欢考“一看就会，一写就废”的题**，考察你代码的**正确性、边界处理、时间/空间复杂度分析**以及**JS 实现细节**。

下面是根据近两年（2024-2025-2026）面经、牛客/力扣/小红书/知乎/Glassdoor 等平台统计，**字节前端最容易/最高频出现的 20 道算法题**（基本都是 LeetCode 原题或轻微变体），难度排序从简单→中等→中等偏上。

我按**字节前端实际出现频率**大致排序（不是 LeetCode 全局热度），并标注最常见考察点。

| 顺序 | LeetCode 编号 | 中文标题                             | 难度      | 字节前端考察频率 | 最常考点 / 字节喜欢怎么变体                      |
| ---- | ------------- | ------------------------------------ | --------- | ---------------- | ------------------------------------------------ |
| 1    | 3             | 无重复字符的最长子串                 | 中等      | ★★★★★            | 滑动窗口 + Map / Set                             |
| 2    | 206 / 25      | 反转链表 / K 个一组翻转链表          | 简单/困难 | ★★★★★            | 链表指针操作（字节超爱链表）                     |
| 3    | 146           | LRU Cache                            | 中等      | ★★★★☆            | Map + 双向链表，手写 get/put                     |
| 4    | 1             | 两数之和                             | 简单      | ★★★★☆            | HashMap（考察基础，但常加 follow-up）            |
| 5    | 415 / 43      | 字符串相加 / 字符串相乘              | 中等      | ★★★★☆            | 大数加法/乘法（字节很喜欢字符串数字题）          |
| 6    | 20            | 有效的括号                           | 简单      | ★★★★☆            | 栈（常变体：包含 \* 的通配符匹配）               |
| 7    | 53            | 最大子数组和                         | 中等      | ★★★★☆            | 贪心 / 前缀和 / 动态规划                         |
| 8    | 215           | 数组中的第K个最大元素                | 中等      | ★★★★☆            | 快选 / 堆 / 排序（字节爱考 Top K 变体）          |
| 9    | 21 / 23       | 合并两个有序链表 / 合并 K 个有序链表 | 简单/困难 | ★★★☆☆            | 链表 + 优先队列（K 路归并很常见）                |
| 10   | 5             | 最长回文子串                         | 中等      | ★★★☆☆            | 中心扩散 / Manacher / DP                         |
| 11   | 121 / 122     | 买卖股票的最佳时机（I/II）           | 简单      | ★★★☆☆            | 一次/多次交易（常一起问）                        |
| 12   | 739           | 每日温度                             | 中等      | ★★★☆☆            | 单调栈（字节很喜欢单调栈题）                     |
| 13   | 438           | 找到字符串中所有字母异位词           | 中等      | ★★★☆☆            | 滑动窗口 + 哈希表                                |
| 14   | 160           | 相交链表                             | 简单      | ★★★☆☆            | 双指针（常问环形链表变体）                       |
| 15   | 200           | 岛屿数量                             | 中等      | ★★★☆☆            | DFS / BFS（网格题入门，字节常考）                |
| 16   | 994           | 腐烂的橘子                           | 中等      | ★★★☆☆            | BFS 多源（网格 + 层序）                          |
| 17   | 42            | 接雨水                               | 困难      | ★★☆☆☆            | 双指针 / 单调栈 / 前后缀最大值                   |
| 18   | 239           | 滑动窗口最大值                       | 困难      | ★★☆☆☆            | 单调队列（字节前端真题概率不低）                 |
| 19   | 76            | 最小覆盖子串                         | 困难      | ★★☆☆☆            | 滑动窗口 + 计数器（困难窗口题代表）              |
| 20   | 912 / 88      | 手写快排 / 合并两个有序数组          | —         | ★★★★☆            | 手写排序算法 + 原地合并（字节爱让手写快排/归并） |

### 快速记忆口诀（字节前端算法 20 题最核心分类）

- 链表：反转、K 翻转、合并、相交、环
- 数组/字符串：滑动窗口（无重复、最小覆盖、字母异位词）、双指针、股票系列
- 哈希/TopK：两数之和、LRU、数组第 K 大
- 栈/队列：括号、每日温度、滑动窗口最大值、单调栈/队列
- 树/回溯/DFS：岛屿数量、腐烂的橘子、最长回文子串（中心扩散也算）
- 手写基础：快排、归并、链表反转、大数加法

### 准备建议（2026 年字节前端现状）

1. **必须 100% 会写** 的前 8 题（尤其是 3、206、146、1、53）
2. **中等偏难窗口/单调结构**（739、438、239、76）至少写出 O(n) 解
3. **链表 + 快慢/双指针** 是字节永恒的爱（206、160、141/142 环形链表）
4. **JS 实现细节**：边界（空数组、空字符串、单一元素）、类型转换、Infinity/NaN
5. **时间不够时** 优先把前 12 题刷 5 遍 + 手写快排/归并 + LRU

如果你时间紧，建议优先顺序：

3 → 206 → 146 → 1 → 53 → 20 → 215 → 739 → 5 → 42

以下是之前列出的 **20 道字节跳动前端最高频算法题** 的 **JavaScript 参考答案**（以 LeetCode 标准解法为主，注重清晰、可读性、边界处理）。这些都是最经典、最容易通过的写法，适合面试直接手写。

我会按原顺序给出，每题包含：

- 简要说明
- JS 代码（ES6+ 风格）
- 时间/空间复杂度

### 1. LeetCode 3 - 无重复字符的最长子串（滑动窗口 + Map/Set）

```javascript
function lengthOfLongestSubstring(s) {
  const seen = new Map();
  let maxLen = 0;
  let left = 0;

  for (let right = 0; right < s.length; right++) {
    const char = s[right];
    if (seen.has(char) && seen.get(char) >= left) {
      left = seen.get(char) + 1;
    }
    seen.set(char, right);
    maxLen = Math.max(maxLen, right - left + 1);
  }
  return maxLen;
}
```

// 时间 O(n)，空间 O(min(n, 字符集大小))

### 2. LeetCode 206 - 反转链表（迭代版，字节最爱）

```javascript
function reverseList(head) {
  let prev = null;
  let curr = head;

  while (curr) {
    const next = curr.next;
    curr.next = prev;
    prev = curr;
    curr = next;
  }
  return prev;
}
```

// 时间 O(n)，空间 O(1)

### 3. LeetCode 146 - LRU Cache（Map + 双向链表）

```javascript
class ListNode {
  constructor(key, value) {
    this.key = key;
    this.value = value;
    this.prev = null;
    this.next = null;
  }
}

class LRUCache {
  constructor(capacity) {
    this.capacity = capacity;
    this.cache = new Map();
    this.head = new ListNode(0, 0); // dummy head
    this.tail = new ListNode(0, 0); // dummy tail
    this.head.next = this.tail;
    this.tail.prev = this.head;
  }

  _addNode(node) {
    node.prev = this.head;
    node.next = this.head.next;
    this.head.next.prev = node;
    this.head.next = node;
  }

  _removeNode(node) {
    node.prev.next = node.next;
    node.next.prev = node.prev;
  }

  _moveToHead(node) {
    this._removeNode(node);
    this._addNode(node);
  }

  get(key) {
    if (!this.cache.has(key)) return -1;
    const node = this.cache.get(key);
    this._moveToHead(node);
    return node.value;
  }

  put(key, value) {
    if (this.cache.has(key)) {
      const node = this.cache.get(key);
      node.value = value;
      this._moveToHead(node);
    } else {
      const newNode = new ListNode(key, value);
      this.cache.set(key, newNode);
      this._addNode(newNode);

      if (this.cache.size > this.capacity) {
        const lru = this.tail.prev;
        this._removeNode(lru);
        this.cache.delete(lru.key);
      }
    }
  }
}
```

// 时间 O(1)，空间 O(capacity)

### 4. LeetCode 1 - 两数之和

```javascript
function twoSum(nums, target) {
  const map = new Map();
  for (let i = 0; i < nums.length; i++) {
    const complement = target - nums[i];
    if (map.has(complement)) {
      return [map.get(complement), i];
    }
    map.set(nums[i], i);
  }
  return [];
}
```

// 时间 O(n)，空间 O(n)

### 5. LeetCode 415 - 字符串相加（大数加法）

```javascript
function addStrings(num1, num2) {
  let i = num1.length - 1;
  let j = num2.length - 1;
  let carry = 0;
  let result = "";

  while (i >= 0 || j >= 0 || carry) {
    const digit1 = i >= 0 ? num1[i--] - "0" : 0;
    const digit2 = j >= 0 ? num2[j--] - "0" : 0;
    const sum = digit1 + digit2 + carry;
    result = (sum % 10) + result;
    carry = Math.floor(sum / 10);
  }
  return result;
}
```

// 时间 O(max(m,n))，空间 O(max(m,n))

### 6. LeetCode 20 - 有效的括号

```javascript
function isValid(s) {
  const stack = [];
  const map = { ")": "(", "}": "{", "]": "[" };

  for (const char of s) {
    if (char === "(" || char === "{" || char === "[") {
      stack.push(char);
    } else {
      if (stack.length === 0 || stack.pop() !== map[char]) {
        return false;
      }
    }
  }
  return stack.length === 0;
}
```

// 时间 O(n)，空间 O(n)

### 7. LeetCode 53 - 最大子数组和（Kadane 算法）

```javascript
function maxSubArray(nums) {
  let maxSoFar = nums[0];
  let maxEndingHere = nums[0];

  for (let i = 1; i < nums.length; i++) {
    maxEndingHere = Math.max(nums[i], maxEndingHere + nums[i]);
    maxSoFar = Math.max(maxSoFar, maxEndingHere);
  }
  return maxSoFar;
}
```

// 时间 O(n)，空间 O(1)

### 8. LeetCode 215 - 数组中的第 K 个最大元素（快选 / 堆）

最常用快选 partition 写法：

```javascript
function findKthLargest(nums, k) {
  function quickSelect(left, right, k) {
    if (left === right) return nums[left];

    const pivotIndex = partition(left, right);
    if (k === pivotIndex) return nums[k];
    else if (k < pivotIndex) return quickSelect(left, pivotIndex - 1, k);
    else return quickSelect(pivotIndex + 1, right, k);
  }

  function partition(left, right) {
    const pivot = nums[right];
    let i = left;
    for (let j = left; j < right; j++) {
      if (nums[j] >= pivot) {
        // 降序 → 第k大
        [nums[i], nums[j]] = [nums[j], nums[i]];
        i++;
      }
    }
    [nums[i], nums[right]] = [nums[right], nums[i]];
    return i;
  }

  return quickSelect(0, nums.length - 1, k - 1);
}
```

// 平均 O(n)，最坏 O(n²)

### 9. LeetCode 23 - 合并 K 个有序链表（使用最小堆）

```javascript
class ListNode {
  constructor(val = 0, next = null) {
    this.val = val;
    this.next = next;
  }
}

function mergeKLists(lists) {
  if (!lists || lists.length === 0) return null;

  // 最小堆：用数组模拟 + sort（面试时允许，实际生产用 PriorityQueue）
  const minHeap = [];

  // 把所有非空链表头节点放入堆
  for (let list of lists) {
    if (list) {
      minHeap.push(list);
    }
  }

  // 按节点值排序（最小堆）
  minHeap.sort((a, b) => a.val - b.val);

  const dummy = new ListNode(0);
  let tail = dummy;

  while (minHeap.length > 0) {
    // 取出最小节点
    const node = minHeap.shift();
    tail.next = node;
    tail = tail.next;

    // 如果有下一个节点，加入堆
    if (node.next) {
      minHeap.push(node.next);
      // 重新排序（模拟堆调整）
      minHeap.sort((a, b) => a.val - b.val);
    }
  }

  return dummy.next;
}
```

// 时间：O(N log k)，N 是所有节点总数，k 是链表数  
// 空间：O(k)

### 10. LeetCode 5 - 最长回文子串（中心扩散法，面试首选）

```javascript
function longestPalindrome(s) {
  if (!s || s.length < 1) return "";

  let start = 0;
  let maxLen = 1;

  function expandAroundCenter(left, right) {
    while (left >= 0 && right < s.length && s[left] === s[right]) {
      const currentLen = right - left + 1;
      if (currentLen > maxLen) {
        start = left;
        maxLen = currentLen;
      }
      left--;
      right++;
    }
  }

  for (let i = 0; i < s.length; i++) {
    // 奇数长度回文（以 i 为中心）
    expandAroundCenter(i, i);
    // 偶数长度回文（以 i 和 i+1 为中心）
    expandAroundCenter(i, i + 1);
  }

  return s.slice(start, start + maxLen);
}
```

// 时间 O(n²)，空间 O(1)

### 11. LeetCode 121 - 买卖股票的最佳时机（一次交易）

```javascript
function maxProfit(prices) {
  if (!prices || prices.length < 2) return 0;

  let minPrice = prices[0];
  let maxProfit = 0;

  for (let i = 1; i < prices.length; i++) {
    if (prices[i] < minPrice) {
      minPrice = prices[i];
    } else {
      maxProfit = Math.max(maxProfit, prices[i] - minPrice);
    }
  }

  return maxProfit;
}
```

// 时间 O(n)，空间 O(1)

### 12. LeetCode 739 - 每日温度（单调栈）

```javascript
function dailyTemperatures(temperatures) {
  const n = temperatures.length;
  const answer = new Array(n).fill(0);
  const stack = []; // 存索引，温度单调递减栈

  for (let curr = 0; curr < n; curr++) {
    while (
      stack.length > 0 &&
      temperatures[stack[stack.length - 1]] < temperatures[curr]
    ) {
      const prev = stack.pop();
      answer[prev] = curr - prev;
    }
    stack.push(curr);
  }

  return answer;
}
```

// 时间 O(n)，空间 O(n)

### 13. LeetCode 438 - 找到字符串中所有字母异位词（滑动窗口）

```javascript
function findAnagrams(s, p) {
  const result = [];
  if (s.length < p.length) return result;

  const pCount = new Array(26).fill(0);
  const sCount = new Array(26).fill(0);

  // 初始化 p 的字符计数
  for (let char of p) {
    pCount[char.charCodeAt(0) - 97]++;
  }

  // 初始化窗口前 p.length 个字符
  for (let i = 0; i < p.length; i++) {
    sCount[s[i].charCodeAt(0) - 97]++;
  }

  if (arraysEqual(pCount, sCount)) {
    result.push(0);
  }

  // 滑动窗口
  for (let i = p.length; i < s.length; i++) {
    // 移除左边字符
    sCount[s[i - p.length].charCodeAt(0) - 97]--;
    // 添加右边字符
    sCount[s[i].charCodeAt(0) - 97]++;

    if (arraysEqual(pCount, sCount)) {
      result.push(i - p.length + 1);
    }
  }

  return result;
}

function arraysEqual(a, b) {
  for (let i = 0; i < 26; i++) {
    if (a[i] !== b[i]) return false;
  }
  return true;
}
```

// 时间 O(n)，空间 O(1)（固定 26 个字母）

### 14. LeetCode 160 - 相交链表（双指针）

```javascript
function getIntersectionNode(headA, headB) {
  if (!headA || !headB) return null;

  let pA = headA;
  let pB = headB;

  while (pA !== pB) {
    pA = pA ? pA.next : headB;
    pB = pB ? pB.next : headA;
  }

  return pA;
}
```

// 时间 O(n + m)，空间 O(1)

### 15. LeetCode 200 - 岛屿数量（DFS）

```javascript
function numIslands(grid) {
  if (!grid || !grid.length) return 0;

  const rows = grid.length;
  const cols = grid[0].length;
  let count = 0;

  function dfs(r, c) {
    if (r < 0 || c < 0 || r >= rows || c >= cols || grid[r][c] !== "1") {
      return;
    }
    grid[r][c] = "0"; // 标记为已访问
    dfs(r - 1, c);
    dfs(r + 1, c);
    dfs(r, c - 1);
    dfs(r, c + 1);
  }

  for (let r = 0; r < rows; r++) {
    for (let c = 0; c < cols; c++) {
      if (grid[r][c] === "1") {
        count++;
        dfs(r, c);
      }
    }
  }

  return count;
}
```

// 时间 O(m*n)，空间 O(m*n)（递归栈）

### 16. LeetCode 994 - 腐烂的橘子（多源 BFS）

```javascript
function orangesRotting(grid) {
  const rows = grid.length;
  const cols = grid[0].length;
  let fresh = 0;
  const queue = [];

  // 统计新鲜橘子，并把所有腐烂橘子入队
  for (let r = 0; r < rows; r++) {
    for (let c = 0; c < cols; c++) {
      if (grid[r][c] === 1) fresh++;
      if (grid[r][c] === 2) queue.push([r, c, 0]); // [r, c, 时间]
    }
  }

  if (fresh === 0) return 0;

  const directions = [
    [-1, 0],
    [1, 0],
    [0, -1],
    [0, 1],
  ];
  let maxTime = 0;

  while (queue.length > 0) {
    const [r, c, time] = queue.shift();
    maxTime = Math.max(maxTime, time);

    for (const [dr, dc] of directions) {
      const nr = r + dr;
      const nc = c + dc;
      if (nr >= 0 && nr < rows && nc >= 0 && nc < cols && grid[nr][nc] === 1) {
        grid[nr][nc] = 2;
        fresh--;
        queue.push([nr, nc, time + 1]);
      }
    }
  }

  return fresh === 0 ? maxTime : -1;
}
```

// 时间 O(m*n)，空间 O(m*n)

### 17. LeetCode 42 - 接雨水（双指针）

```javascript
function trap(height) {
  if (!height || height.length < 3) return 0;

  let left = 0;
  let right = height.length - 1;
  let leftMax = 0;
  let rightMax = 0;
  let water = 0;

  while (left < right) {
    if (height[left] <= height[right]) {
      if (height[left] >= leftMax) {
        leftMax = height[left];
      } else {
        water += leftMax - height[left];
      }
      left++;
    } else {
      if (height[right] >= rightMax) {
        rightMax = height[right];
      } else {
        water += rightMax - height[right];
      }
      right--;
    }
  }

  return water;
}
```

// 时间 O(n)，空间 O(1)

### 18. LeetCode 239 - 滑动窗口最大值（单调队列）

```javascript
function maxSlidingWindow(nums, k) {
  if (!nums || nums.length === 0) return [];
  if (k === 1) return nums;

  const result = [];
  const deque = []; // 存索引，单调递减

  for (let i = 0; i < nums.length; i++) {
    // 移除窗口外的元素
    if (deque.length > 0 && deque[0] <= i - k) {
      deque.shift();
    }

    // 移除比当前元素小的（它们不可能成为最大值）
    while (deque.length > 0 && nums[deque[deque.length - 1]] < nums[i]) {
      deque.pop();
    }

    deque.push(i);

    // 窗口形成后记录最大值
    if (i >= k - 1) {
      result.push(nums[deque[0]]);
    }
  }

  return result;
}
```

// 时间 O(n)，空间 O(k)

### 19. LeetCode 76 - 最小覆盖子串（滑动窗口）

```javascript
function minWindow(s, t) {
  if (!s || !t || s.length < t.length) return "";

  const tCount = new Map();
  for (let char of t) {
    tCount.set(char, (tCount.get(char) || 0) + 1);
  }

  let required = tCount.size;
  let formed = 0;
  const windowCounts = new Map();
  let left = 0;
  let minLen = Infinity;
  let minStart = 0;

  for (let right = 0; right < s.length; right++) {
    const char = s[right];
    windowCounts.set(char, (windowCounts.get(char) || 0) + 1);

    if (tCount.has(char) && windowCounts.get(char) === tCount.get(char)) {
      formed++;
    }

    while (left <= right && formed === required) {
      const currentLen = right - left + 1;
      if (currentLen < minLen) {
        minLen = currentLen;
        minStart = left;
      }

      const leftChar = s[left];
      windowCounts.set(leftChar, windowCounts.get(leftChar) - 1);

      if (
        tCount.has(leftChar) &&
        windowCounts.get(leftChar) < tCount.get(leftChar)
      ) {
        formed--;
      }
      left++;
    }
  }

  return minLen === Infinity ? "" : s.slice(minStart, minStart + minLen);
}
```

// 时间 O(|s| + |t|)，空间 O(字符集大小)

### 20. 手写快排（经典递归版）

```javascript
function quickSort(arr) {
  if (arr.length <= 1) return arr;

  const pivot = arr[arr.length - 1];
  const left = [];
  const right = [];

  for (let i = 0; i < arr.length - 1; i++) {
    if (arr[i] < pivot) {
      left.push(arr[i]);
    } else {
      right.push(arr[i]);
    }
  }

  return [...quickSort(left), pivot, ...quickSort(right)];
}

// 原地版（面试更常见）
function quickSortInPlace(arr, left = 0, right = arr.length - 1) {
  if (left >= right) return arr;

  const pivotIndex = partition(arr, left, right);
  quickSortInPlace(arr, left, pivotIndex - 1);
  quickSortInPlace(arr, pivotIndex + 1, right);

  return arr;
}

function partition(arr, left, right) {
  const pivot = arr[right];
  let i = left - 1;

  for (let j = left; j < right; j++) {
    if (arr[j] <= pivot) {
      i++;
      [arr[i], arr[j]] = [arr[j], arr[i]];
    }
  }
  [arr[i + 1], arr[right]] = [arr[right], arr[i + 1]];
  return i + 1;
}
```

这些代码在面试中基本都能直接使用。  
如果想针对某几题再深入讨论（字节常见 follow-up、优化、易错点），可以告诉我具体题号～

```

这些代码在 LeetCode 上基本都能 AC，字节面试时注意：

- 边界（空输入、单一元素、负数、全相同等）
- 变量命名清晰
- 口述复杂度 + 优化思路

想看哪几道更详细的字节变体 / 优化写法 / 错误易错点？直接告诉我题号就好～
```
